node_modules/
dist/
.data/
*.log
.turbo/
.verify-shots/*.tsbuildinfo

# Brand assets are RENDERED at build time by packages/client/scripts/
# generate-brand.mjs — derived artifacts, so they don't belong in git.
packages/client/public/icons/
